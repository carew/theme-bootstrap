{% extends "default.html.twig" %}

{% block content_main %}
  <ul>
    {% for post  in posts %}
      <li>
        <a href="{{ post.path }}">{{ post.metadat.title|title }}</a>
      </li>
    {% endfor %}
  </ul>
{% endblock %}
